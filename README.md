# Dotfiles

Quickshell configuration managed with GNU Stow.

Install from this directory:

```sh
stow --no-folding --target="$HOME" quickshell
```

Runtime files such as `.qmlls.ini` remain in `~/.config/quickshell`.
